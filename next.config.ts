// next.config.ts

const nextConfig = {
/*   experimental: {
    // Required for Turbopack to transpile non-Next packages
    // Especially if you're working with ESM-only packages
    transpilePackages: ['@noir-lang/noirc_abi', '@noir-lang/acvm_js'],
  }, */
};

export default nextConfig;
